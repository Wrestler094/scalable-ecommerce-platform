# ======== APP CONFIG ========
APP_NAME=catalog-service
APP_VERSION=1.0.0
HTTP_PORT=8080
# ↓ Docker Compose only (not used in app code)
HTTP_EXTERNAL_PORT=8082

# ======== JWT ========
ACCESS_SECRET=super-secret-string
TOKEN_TTL=900

# ======== LOGGING ========
LOG_LEVEL=debug

# ======== DATABASE (PostgreSQL) ========
DB_HOST=catalog-db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=products_db
# ↓ Docker Compose only (not used in app code)
DB_EXTERNAL_PORT=5434

# ======== FEATURES (internal toggles) ========
METRICS_ENABLED=false
SWAGGER_ENABLED=false
